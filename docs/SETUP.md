# 🚀 自動セットアップ・デプロイガイド

このプロジェクトは**完全自動化**されています。1つのコマンドで全て完了します。

---

## ⚡ クイックスタート（3ステップ）

### ステップ1: GitHubトークンを取得

1. https://github.com/settings/tokens にアクセス
2. "Generate new token (classic)" をクリック
3. Scopes: `repo`, `workflow` を選択
4. トークンをコピー（`ghp_`で始まる）

### ステップ2: トークンを保存

```bash
cd /home/user/webapp
echo "GITHUB_TOKEN=ghp_your_actual_token_here" > .env
```

### ステップ3: 自動デプロイ実行

```bash
bash auto_deploy_all.sh
```

**これだけです！** 🎉

スクリプトが以下を自動実行します：
- ✅ 全リポジトリをGitHubにプッシュ
- ✅ Pull Requestを自動作成
- ✅ PRを自動マージ
- ✅ 完了通知

---

## 🔄 ワークフロー理解

### 現在の状態
```
✅ enterprise-rag-system      (コミット済み、ローカル)
✅ llm-agent-framework        (コミット済み、ローカル)
✅ realtime-edge-detection    (コミット済み、ローカル)
✅ multilingual-sentiment-analyzer (コミット済み、ローカル)
✅ micro-instruction-engineering   (コミット済み、ローカル)
```

### 実行後の状態
```
✅ enterprise-rag-system      (GitHub main にマージ済み)
✅ llm-agent-framework        (GitHub main にマージ済み)
✅ realtime-edge-detection    (GitHub main にマージ済み)
✅ multilingual-sentiment-analyzer (GitHub main にマージ済み)
✅ micro-instruction-engineering   (GitHub main にマージ済み)
```

---

## 📋 詳細な使用方法

### 方法1: .envファイルを使用（推奨）

```bash
# トークンを.envに保存
echo "GITHUB_TOKEN=ghp_your_token" > .env

# 実行
bash auto_deploy_all.sh
```

### 方法2: 直接トークンを指定

```bash
bash auto_deploy_all.sh ghp_your_token_here
```

### 方法3: GitHub CLI を使用（最速）

```bash
# GitHub CLI をインストール済みの場合
gh auth login

# 実行（トークン不要）
bash auto_deploy_all.sh
```

---

## 🛠️ スクリプトが実行すること

各リポジトリに対して：

1. **プッシュ** (`git push origin develop -f`)
   - developブランチをGitHubにプッシュ
   
2. **PR作成** (GitHub API)
   - develop → main のPull Requestを作成
   - タイトル: "feat: Initial implementation - {repo}"
   - 自動生成された説明文
   
3. **マージ** (GitHub API)
   - PRを自動的にマージ
   - developブランチを削除

4. **確認**
   - 成功/失敗をレポート
   - 統計情報を表示

---

## 🔧 トラブルシューティング

### Q: "Authentication failed" エラー

**A:** トークンが正しいか確認
```bash
# トークンの確認
cat .env

# 再設定
echo "GITHUB_TOKEN=ghp_new_token" > .env
```

### Q: "PR already exists" エラー

**A:** 既存のPRを使用します（正常動作）
- スクリプトが既存PRを検出してマージします

### Q: "Merge conflict" エラー

**A:** 手動でコンフリクトを解決
```bash
cd repository-name
git pull origin main
# コンフリクト解決
git push origin develop
```

その後、再度スクリプト実行

### Q: 一部のリポジトリが失敗

**A:** 失敗したリポジトリのみ再実行
```bash
# 個別に実行
cd failed-repo
git push origin develop -f
# 手動でPR作成・マージ
```

---

## 📊 スクリプト出力例

```
╔══════════════════════════════════════════════════════════════╗
║   Automatic Deploy All Repositories to GitHub               ║
║   Push → Create PR → Merge                                   ║
╚══════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 Deploying: enterprise-rag-system
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📤 Step 1/3: Pushing to GitHub...
✅ Pushed successfully
📝 Step 2/3: Creating Pull Request...
✅ PR #1 created
🔀 Step 3/3: Merging Pull Request...
✅ PR merged successfully
✅ Branch deleted
🎉 Deployment complete for enterprise-rag-system

[... 他のリポジトリも同様 ...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Deployment Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Successfully deployed: 5

🎉 All repositories deployed successfully!

✅ Check your repositories:
   https://github.com/jinno-ai
```

---

## 🎯 今後の開発フロー

### 新しい変更をデプロイする場合

1. **コードを変更**
   ```bash
   cd repository-name
   # コード編集
   ```

2. **コミット**
   ```bash
   git add .
   git commit -m "feat: new feature"
   ```

3. **自動デプロイ**
   ```bash
   cd /home/user/webapp
   bash auto_deploy_all.sh
   ```

### 個別リポジトリのみデプロイ

```bash
cd repository-name
git push origin develop
# GitHub UIでPR作成・マージ
```

---

## 📚 関連スクリプト

| スクリプト | 用途 |
|-----------|------|
| `auto_deploy_all.sh` | **全自動デプロイ（Push→PR→Merge）** |
| `push_all_repos.sh` | プッシュのみ（PR作成は手動） |
| `check_setup.py` | セットアップ状態確認 |
| `dev_workflow.py` | 開発ワークフロー管理 |

---

## 🔐 セキュリティ

### トークンの保護

- `.env` ファイルは `.gitignore` に含まれています
- ローカルにのみ保存されます
- **絶対に公開リポジトリにコミットしないでください**

### トークンのローテーション

90日ごとに新しいトークンを生成することを推奨：

```bash
# 新しいトークンを取得
# https://github.com/settings/tokens

# .env を更新
echo "GITHUB_TOKEN=ghp_new_token" > .env
```

---

## ✅ チェックリスト

初回セットアップ時：

- [ ] GitHubトークンを取得
- [ ] `.env` ファイルを作成
- [ ] `auto_deploy_all.sh` を実行
- [ ] すべてのリポジトリがマージされたことを確認
- [ ] https://github.com/jinno-ai でリポジトリを確認

---

## 🎊 完了！

これで全自動デプロイシステムが完成しました。

今後は、コード変更後に以下を実行するだけです：

```bash
bash auto_deploy_all.sh
```

すべてが自動で完了します！ 🚀

---

## 💡 ヒント

### さらに効率化

エイリアスを設定：

```bash
echo 'alias deploy="cd /home/user/webapp && bash auto_deploy_all.sh"' >> ~/.bashrc
source ~/.bashrc

# 使用
deploy
```

### 定期的な自動デプロイ

Cron ジョブで設定：

```bash
# 毎日夜中にデプロイ
0 0 * * * cd /home/user/webapp && bash auto_deploy_all.sh
```

---

**Happy Deploying! 🎉**
